# Jolene PWA (React + Tailwind v3)

## Quick start
npm install
npm start

## Build PWA + test
npm run build
npx serve -s build

Manifest at public/manifest.webmanifest, SW at public/sw.js, icons in public/icons/.
